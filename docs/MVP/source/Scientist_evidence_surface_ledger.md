# Scientist Evidence Surface Ledger

Updated: May 8, 2026

This ledger tracks what the current MVP genuinely proves, where the proof lives, and how it should be described in a scientist discussion.

Primary verification sources:
- hosted `/` route smoke on `2026-05-08`
- hosted `/admin` route or gate smoke on `2026-05-08`
- hosted `run-forecast` smoke on `2026-05-08`: `status=ready`, `stale=false`, `sameDayPublished=true`, `forecastDate=2026-05-08`, `forecastRunId=4822ecf8-defa-4479-ac86-cf9eb7cf2f08`, `publishedAt=2026-05-08T14:31:50.594343+00:00`, `horizonHours=72`, `20x20`, `400` ready cells, `0` stale cells, structured bulletin present, and no synthetic inputs
- active artifact explanation/runout boundary on `2026-05-08`: `explainability_mode=heuristic_fallback`, `skipTreeShap=true`, and `runout_method_counts={"alpha_beta_elliptical":7}`; WhiteboxTools smoke passed separately but is not the active public artifact runout method
- hosted authenticated `/admin` smoke on `2026-05-08`
- `backend/artifacts/20260504T070406Z/inference_manifest.json`
- `backend/artifacts/20260504T070406Z/stability_summary.json`
- targeted frontend / backend / Deno tests

| Capability | Current proof | Route or artifact | Observed limitation | Scientist-safe wording | Gap severity (1-5) |
|---|---|---|---|---|---:|
| Forecast workspace | Public route loads a batch-first forecast shell with region selector, timeline, share/export/report actions, and stateful model badge. The May 8 `run-forecast` smoke returned same-day full-grid cell publication evidence for Colorado Rockies. | Hosted `/`; `run-forecast`; `src/pages/Index.tsx` | Current proof is operational-shell and technical publication proof, not scientist validation proof. | “The MVP proves a usable artifact-driven forecast workspace for the current published batch; the current May 8 proof is same-day full-grid technical publication evidence.” | 1 |
| Bulletin semantics | The app implements danger level, problem framing, prone location, daypart chips, and reduced-confidence wording, and the May 8 full-grid artifact includes structured bulletin content. | `src/components/ForecastBulletinBadge.tsx`; `src/test/forecast-bulletin-badge.test.tsx`; latest May 8 full-grid hosted screenshots | Bulletin presence proves product semantics and publication packaging, not field-validated danger correctness. | “The public bulletin structure is implemented and explicit, but remains `EAWS-style experimental` until scientist review closes.” | 2 |
| Masking / terrain eligibility | Public map differentiates masked terrain from danger classes. | Hosted `/`; `Index.tsx`; public mask profiles in current code and artifacts | Terrain masking reduces false confidence but does not close snowline / local-terrain science. | “Masked terrain indicates out-of-scope or low-confidence terrain, not low risk.” | 2 |
| Uncertainty cues | Reduced-confidence badge and high-uncertainty cell counts are visible on the public route. | Hosted `/`; bulletin confidence caption; tests | Current cues communicate bounded honesty, not full uncertainty quantification. | “The MVP communicates uncertainty explicitly where support is thin.” | 2 |
| Share / export / report | Public controls for share, CSV, JSON, and report are present and routable, with export now disabled until a published forecast artifact is loaded. | Hosted `/`; `FieldReportForm`; `ExportForecast`; targeted frontend tests | These are product controls, not proof of downstream institutional workflow adoption; export proof depends on artifact availability. | “Teams can share state and submit reports from the current surface; export requires a loaded published artifact.” | 2 |
| Field-report flow | Public report intake is implemented and regression-tested. | Hosted `/`; `src/components/FieldReportForm.tsx`; `src/test/field-report-form.test.tsx` | The operator lane currently shows no recent field reports in the sampled admin view, and offline queue-and-replay needs device/deployment smoke before being treated as field-reliability proof. | “Field-report intake exists; downstream governance evidence remains sample-dependent.” | 3 |
| Public model-status summary | The public route shows active scorer, candidate scorer, release evidence, evidence mix, drift mode, stability, and benchmark summary, grouped into current proof, publication state, candidate path, release gates, evidence mix, benchmark, and governance scope. | Hosted `/`; `src/components/ModelStatusBadge.tsx`; targeted test | Public copy can support credibility only if it keeps current proof separate from candidate/gated paths. | “The public status badge is governance-aware summary language, not activation proof.” | 2 |
| Source health | Admin shows support status, completeness, freshness, SAR coverage mode, and missing inputs for the freshest publication row. | Hosted `/admin` route and authenticated smoke on `2026-05-08`; `forecast_runs.model_metadata.source_health`; `docs/OPERATOR_ROLLOUT_QA.md` | Operator-only surface; depends on using the freshest `model_status` / publication rows. | “Operator source-health observability exists and is now freshness-ordered.” | 2 |
| Decision provenance | Admin shows threshold profile, origin, dominant mapping, aggregation, calibration, and selected-feature count. | Hosted `/admin` route and authenticated smoke on `2026-05-08`; `forecast_runs.model_metadata.decision_provenance`; backend tests | Provenance improves inspectability but does not prove scientific correctness. | “Decision provenance is inspectable and bounded to operator governance.” | 2 |
| Runtime benchmark traces | Benchmark harness can summarize training and inference publication timings from current artifacts. | `backend/scripts/run_pipeline_benchmarks.py`; `training_stage_metrics.json`; `inference_manifest.json`; hosted `/admin` | Runtime traces are operational observability, not field-validation metrics. | “We can prove how the current pipeline ran, not that it is scientifically validated.” | 2 |
| Stability summary | Current artifact pack proves `stability_summary.classification = unstable` over `3` seeds with `threshold_drift = 0.157726`. | `backend/artifacts/20260504T070406Z/stability_summary.json`; hosted `/admin`; backend tests | This is bounded seed-stability evidence, not exhaustive robustness validation. | “Current stability evidence is explicit and conservative; it should be read as governance evidence.” | 3 |
| Evaluation contract | Deno tests prove outcome-labeling and slice-metric contracts; admin evaluation sections exist. | Deno tests; hosted `/admin`; `run-evaluation` contract | Live evaluation tables are sparse / empty in the current smoke, so the artifact path is stronger than the live operator count. | “Evaluation plumbing is implemented, but the current live view is not yet a rich scientist benchmark surface.” | 3 |
| Autonomous evidence mix | Admin and artifact pack show positive counts, manual vs autonomous mix, source-region coverage, and promoted SAR counts. | Hosted `/admin` route or gate; `autonomous_evidence_summary`; `inference_manifest.json` | Current local artifact is `1000/1000` autonomous with `0` manual positives, `6` regions, only `gee_sar` positive sources, and newest source timestamp `2023-12-24T00:08:24.600000+00:00`, so the evidence mix is narrow and time-bounded. | “Current evidence fusion is visible and governed, but the active artifact mix is still narrow and should not be oversold.” | 4 |
| Shadow-path qualification | Candidate model gate, ready-for-activation flag, and SAR-volume thresholds are explicit in artifacts and admin. | Hosted `/admin`; `dynamic_model_candidate`; `inference_manifest.json` | The current candidate remains blocked by `shadow_quality_gate`; SAR release / volume gates also remain unmet. | “The shadow path is real and inspectable, but it is not promoted.” | 4 |
