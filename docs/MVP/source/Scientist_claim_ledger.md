# Scientist Claim Ledger

Updated: May 8, 2026

This ledger is the scientist-meeting source of truth for what can be said now, what requires caveats, and what remains blocked.

Evidence sources used:
- hosted `/` route smoke on `2026-05-08`
- hosted `/admin` route or gate smoke on `2026-05-08`
- hosted `run-forecast` smoke on `2026-05-08`: current Colorado Rockies artifact returned `status=ready`, `stale=false`, `sameDayPublished=true`, `forecastDate=2026-05-08`, `forecastRunId=4822ecf8-defa-4479-ac86-cf9eb7cf2f08`, `publishedAt=2026-05-08T14:31:50.594343+00:00`, `horizonHours=72`, `20x20`, `400` ready cells, `0` stale cells, structured bulletin present, and no synthetic inputs
- active artifact explanation/runout boundary on `2026-05-08`: `explainability_mode=heuristic_fallback`, `skipTreeShap=true`, and `runout_method_counts={"alpha_beta_elliptical":7}`; WhiteboxTools smoke passed separately but is not the active public artifact runout method
- hosted authenticated `/admin` smoke on `2026-05-08`: signed-in dashboard proved the observability lane and the refreshed screenshot shows the exact active full-grid run id
- `docs/OPERATOR_ROLLOUT_QA.md`
- `backend/artifacts/20260504T070406Z/inference_manifest.json`
- `backend/artifacts/20260504T070406Z/stability_summary.json`
- current UI components and regression tests

| Statement | Claim state | Proof tier | Proving artifact or route | Blocked phrasing | Safe phrasing | Owner | Status |
|---|---|---|---|---|---|---|---|
| We already have a usable avalanche forecast decision-support shell. | `Active` | `Live demo` | Hosted `/`; `src/pages/Index.tsx`; `ModelStatusBadge`; forecast workspace controls; hosted `run-forecast` same-day response | “fully autonomous avalanche AI”; “full-grid scientist-validated forecast is complete” | “batch-first decision-support forecast workspace for the current published artifact; current May 8 proof is same-day full-grid technical publication evidence” | Product + Frontend | `Meeting-safe with validation caveat` |
| The public bulletin uses disciplined avalanche-bulletin structure. | `Active with validation caveat` | `Live demo` plus `Repo/admin verified` | `src/components/ForecastBulletinBadge.tsx`; targeted tests; May 8 full-grid hosted screenshots | “official avalanche warning service”; “the May 8 full-grid artifact is scientist-validated” | “`EAWS-style experimental` bulletin framing is implemented and present in the May 8 full-grid publication; correctness remains subject to scientist review” | Product + Frontend | `Meeting-safe with validation caveat` |
| Out-of-scope terrain is masked instead of flattened into low danger. | `Active` | `Live demo` | Hosted `/`; public map semantics; `Index.tsx` masking path | “terrain truth is fully solved” | “masked terrain semantics reduce false confidence outside eligible terrain” | Frontend + Geospatial | `Meeting-safe` |
| Reduced-confidence states are visible to the user. | `Active` | `Live demo` | Hosted `/`; `ForecastBulletinBadge`; uncertainty caption and confidence badge | “full probabilistic uncertainty science stack” | “reduced-confidence and high-uncertainty cues are surfaced in the public product” | Frontend + Product | `Meeting-safe` |
| Operators can share, export, and report from the current forecast surface. | `Active` | `Live demo` | Hosted `/`; share / CSV / JSON / report controls | “all downstream workflows are validated in field operations”; “exports are always available even without a loaded artifact” | “share/export/report actions exist in the current forecast shell; export requires a loaded published forecast artifact” | Frontend | `Meeting-safe` |
| Hazard can be viewed with consequence-aware overlays and expert controls. | `Conditionally available` | `Live demo` | Hosted `/` expert-mode controls; existing overlay paths | “complete regional consequence modelling” | “expert overlays and consequence-aware review are available where the underlying data are present” | Frontend + Geospatial | `Needs caveat` |
| Forecast reasoning and operational provenance are inspectable. | `Conditionally available` | `Repo/admin verified` | Hosted `/admin` route and authenticated smoke on `2026-05-08`; `Source Health`; `Decision Provenance`; `forecast_runs.model_metadata` | “the model is correct because it is explainable” | “source health, decision provenance, and operator observability are inspectable in the signed-in admin lane; this is governance evidence, not scientific correctness proof” | Backend + Frontend | `Needs caveat` |
| Field reports can be captured from the product today. | `Active` | `Live demo` | Hosted `/`; `FieldReportForm`; `src/test/field-report-form.test.tsx` | “field-ready offline mobile reliability is fully proven” | “field-report intake exists, and local queue-and-replay capability is implemented; field-device reliability remains a separate operational smoke question” | Frontend + Ops | `Meeting-safe` |
| Autonomous evidence is governed before it affects training trust. | `Candidate` | `Repo/admin verified` | `backend/common/label_governance.py`; hosted `/admin`; `autonomous_evidence_summary` | “autonomous truth generation is solved” | “autonomous evidence is weighted, decayed, and gate-checked before it is trusted” | ML/Data + Product | `Needs caveat` |
| A next-generation dynamic model candidate exists in shadow form. | `Candidate` | `Repo/admin verified` | Hosted `/admin`; `dynamic_model_candidate`; `inference_manifest.json` | “active public MTS-LSTM scoring” | “an `mts_lstm_v1` candidate is visible, but it remains blocked by release gates” | ML/Data | `Needs caveat` |
| Runtime benchmarking exists for the current publication pipeline. | `Conditionally available` | `Repo/admin verified` | `run_pipeline_benchmarks.py`; `inference_manifest.json`; hosted `/admin` benchmark display | “we have scientist-grade field validation benchmarks already” | “we have reproducible runtime and publication benchmark traces for the current pipeline” | Backend + Ops | `Needs caveat` |
| Stability evidence exists for the current model family. | `Conditionally available` | `Repo/admin verified` | `stability_summary.json`; hosted `/admin` Model Stability | “the model is stable across all conditions” | “current stability evidence is conservative and bounded: `unstable` over `3` seeds with threshold drift, which is governance evidence rather than broad validation” | ML/Data | `Needs caveat` |
| Authoritative SAR support is already operational. | `Unavailable` | `Repo/admin verified` | Hosted `/admin` satellite stats; `dynamic_model_candidate` gates; `docs/OPERATOR_ROLLOUT_QA.md` | “authoritative SAR” | “SAR remains artifact-gated and unpromoted in the current MVP” | ML/Data + Geospatial | `Blocked` |
| Whitebox runout is operationally qualified. | `Exploratory` | `Repo/admin verified` | WhiteboxTools smoke passed separately on May 8; active public artifact still reports analytical Alpha-Beta fallback counts | “operational whitebox runout” | “runout remains exploratory: WhiteboxTools smoke has passed, but the active public artifact still uses analytical Alpha-Beta fallback until full validation and publication succeed” | Geospatial + Backend | `Blocked` |
| Critical-layer / weak-layer science closure is already in place. | `Unavailable` | `Shadow-gated or config-gated` | Current docs, artifact pack, and tests show no scientist-approved critical-layer review loop | “critical-layer science is solved” | “critical-layer and weak-layer validation remain open scientist-review questions” | Scientist + ML/Data | `Blocked` |
| The product has authority-grade warning posture today. | `Unavailable` | `Shadow-gated or config-gated` | Current docs; hosted public semantics; `OPERATOR_ROLLOUT_QA` release gates | “operational warning service” | “honesty-first decision support MVP, not authority-grade warning service” | Product | `Blocked` |
