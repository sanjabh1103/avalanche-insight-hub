name: Backend CI

on:
  pull_request:
    paths:
      - 'backend/**'
      - 'supabase/functions/**'
      - '.github/workflows/**'
      - 'docs/MODAL_WORKER.md'
      - 'CODEOWNERS'
  push:
    branches:
      - main
      - master
    paths:
      - 'backend/**'
      - 'supabase/functions/**'
      - '.github/workflows/**'
      - 'docs/MODAL_WORKER.md'
      - 'CODEOWNERS'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Set up Python 3.11
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.11'

      - name: Install verification dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install "fastapi[standard]>=0.115.0" "modal>=0.73.82" "PyYAML>=6.0.2"

      - name: Validate workflow YAML syntax
        run: |
          python - <<'PY'
          from pathlib import Path
          import yaml

          for path in sorted(Path('.github/workflows').glob('*.yml')):
              with path.open('r', encoding='utf-8') as handle:
                  yaml.safe_load(handle)
          print('workflow yaml ok')
          PY

      - name: Modal app import smoke
        env:
          PYTHONPATH: ${{ github.workspace }}
          MODAL_WORKER_TOKEN: ci-smoke-token
        run: |
          python - <<'PY'
          from backend.modal_worker_app import create_fastapi_app

          app = create_fastapi_app()
          routes = {route.path for route in app.routes}
          expected = {'/sar-segment', '/train-mtslstm', '/infer-mtslstm', '/evaluate-release'}
          missing = expected - routes
          if missing:
              raise SystemExit(f'missing modal worker routes: {sorted(missing)}')
          print('modal app routes ok')
          PY

      - name: Run backend unit and gate-lock tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python -m unittest discover -s backend/tests -p 'test_*.py'
